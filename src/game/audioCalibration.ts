let audioCalibrationOffset = 0;

export function getAudioCalibrationOffset(): number {
  return audioCalibrationOffset;
}

export function setAudioCalibrationOffset(offset: number) {
  audioCalibrationOffset = offset;
}
